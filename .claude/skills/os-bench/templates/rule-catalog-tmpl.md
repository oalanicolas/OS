# Rule / Invariant Catalog: {SUBJECT_NAME}

**Subject:** {SUBJECT_SLUG}
**Date:** {DATE}
**Extraction confidence:** {HIGH/MEDIUM/LOW}

---

> Uso: gerar apenas quando o subject tem invariantes formais explícitas (ex: orchestration frameworks com policies, coding agents com lifecycle gates, workflow infra com retry semantics). Para repos puramente imperativos/tooling, pular este artefato.

## 1. Domain / Bounded Context

| Field | Value |
|-------|-------|
| Primary domain | {ex: agent_orchestration, memory_persistence, mcp_protocol} |
| Bounded contexts | {LIST} |
| Upstream deps | {LIST_OR_NONE} |
| Downstream consumers | {LIST_OR_NONE} |

## 2. Glossary

Termos usados nas invariantes do subject. Um termo não definido aqui **não** pode aparecer em rule statements.

| Term | Definition | Source |
|------|-----------|--------|
| {term} | {1_line_def} | `OS/{subject}/{path}` |

## 3. Invariants (Rules)

Apenas regras verificáveis — observáveis no filesystem ou nos docs. Nada inventado.

### 3.1 Structural (what IS)

| ID | Statement | Evidence |
|----|-----------|----------|
| INV-{NNN} | {statement_in_plain_language} | `OS/{subject}/{path}:{line}` |

### 3.2 Behavioral (what MUST be)

| ID | Type | Statement | Evidence |
|----|------|-----------|----------|
| INV-{NNN} | constraint / prohibition / enabler | {statement} | `OS/{subject}/{path}:{line}` |

**Type guide:**
- `constraint` — restringe o que é permitido
- `prohibition` — proíbe explicitamente
- `enabler` — concede permissão
- `computation` — calcula um valor com obrigação

### 3.3 Decision / Policy (how decisions are made)

| ID | Statement | Referenced table | Evidence |
|----|-----------|------------------|----------|
| INV-{NNN} | {statement} | {DT_ID_OR_NONE} | `OS/{subject}/{path}:{line}` |

## 4. Decision Tables (optional)

Se o subject tem decision tables formais (ex: retry matrix, priority matrix):

### DT-{ID}: {decision_name}

**Hit policy:** {U | F | P | A | C}
**Source:** `OS/{subject}/{path}:{line}`

| {condition_1} | {condition_2} | → {outcome} |
|:-------------|:-------------|:------------|
| {val} | {val} | {val} |

Hit policy reference:
- U (Unique): exatamente uma linha faz match
- F (First): primeira linha que bate ganha
- P (Priority): output com maior prioridade declarada ganha
- A (Any): todas as linhas em match devem concordar no output
- C (Collect): todos os outputs coletados

## 5. Rule Details (YAML)

```yaml
# Um bloco por invariante
- rule:
    id: "INV-001"
    name: "{descriptive_name}"
    type: "constraint | computation | inference | enabler | prohibition | definition | derivation"
    statement: |
      {plain_language_statement}
    source:
      file: "OS/{subject}/{path}"
      line: {N}
      snippet: |
        {verbatim_code_snippet}
    evidence_confidence: "HIGH | MEDIUM | LOW"
```

## 6. Traceability

| Rule ID | Source file | Line | Module | Notes |
|---------|-------------|------|--------|-------|
| INV-{NNN} | `OS/{subject}/{path}` | {N} | {module} | {notes} |

## 7. Open Questions

Regras que apareceram no código/docs mas não estão completamente verificadas:

| ID | Description | Status |
|----|-------------|--------|
| Q-{NNN} | {question} | open / investigating / resolved |

---

## Extraction Notes

- Scanned: {LIST}
- Skipped: {LIST_WITH_REASON}
- Confidence: {HIGH/MED/LOW}

---

_Generated by os-bench inventory task (optional rule catalog step) | Template v1.0_
